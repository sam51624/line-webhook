from flask import Flask, request

app = Flask(__name__)

@app.route('/')
def index():
    return "LINE Webhook is running!"

@app.route('/webhook', methods=['POST'])
def webhook():
    data = request.json
    print("📥 ข้อมูลที่ได้รับจาก LINE:", data)
    return 'OK'
